({
	init: function (cmp, event, helper) {
		helper.Init(cmp,event,helper);
	},

	associaMotivo: function (cmp, event, helper) {
		helper.associaMotivo(cmp, event, helper);
	},

	rimuoviSelezionati: function (cmp, event, helper) {
		helper.rimuoviSelezionati(cmp, event, helper);
	},

	verificaSelezione: function (cmp, event, helper) {
		helper.verificaSelezione(cmp, event, helper);
    }
})